# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Vercel Hosting on merge
on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Hit API
        run: curl -X POST https://api.vercel.com/v1/integrations/deploy/prj_MljRj2MD13H4qX8XB1uN4SpWS1PH/s844SpmQPD

  restart_server:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd ~/PrintEasyBackend
            if [ ! -f "./deploy.sh" ]; then
              echo "Error: deploy.sh not found"
              exit 1
            fi
            chmod +x ./deploy.sh
            ./deploy.sh
